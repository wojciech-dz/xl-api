<form method="post" action="{{ path('app_invoices_invoice_items_delete', {'id': invoice_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ invoice_item.id) }}">
    <button class="btn">Delete</button>
</form>
